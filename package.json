{
  "name": "tdarr-plugins",
  "version": "1.0.0",
  "private": true,
  "description": "AV1 encoding FlowPlugins for Tdarr (av1an + ab-av1)",
  "scripts": {
    "build": "./build.sh",
    "deploy": "./build.sh --deploy"
  },
  "devDependencies": {
    "esbuild": "^0.25"
  }
}
